/**
 * PARTICIPANTS MODULE - Database Queries
 */

import {
  Participant,
  CreateParticipantInput,
  UpdateParticipantInput,
} from "./types";

// TODO: Implement after Drizzle ORM is configured
// export const getParticipantsByTripId = async (tripId: string): Promise<Participant[]> => { ... }
// export const getParticipantById = async (id: string): Promise<Participant | null> => { ... }
// export const createParticipant = async (input: CreateParticipantInput): Promise<Participant> => { ... }
// export const updateParticipant = async (id: string, input: UpdateParticipantInput): Promise<Participant> => { ... }
// export const deleteParticipant = async (id: string): Promise<void> => { ... }
